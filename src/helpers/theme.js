import React from 'react'
import { useDimensions, useDeviceOrientation } from '@react-native-community/hooks'

const padXM = 15

export const theme = {
  dark: "#171716",
  darkGrey: "#82817B",
  padXM,
}